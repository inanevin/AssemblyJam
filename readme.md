# School Massage


## Build

### macOS
```
mkdir build
cd build
cmake .. -G Xcode
```

- open the `AssemblyJam.xcodeproj`
- select the binary `Edit Scheme` > `Run` > `Info` > `Executable` > `SchoolMassage`
- ???
- shoot the fuckers